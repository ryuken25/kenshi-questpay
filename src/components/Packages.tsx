import Link from "next/link";
import { SERVICES } from "@/lib/services";
export default function Packages() { return <section className="section-padding"><div className="mx-auto max-w-6xl"><p className="font-mono text-xs font-bold uppercase tracking-[0.2em] text-[#8FEAFF]">Productized services</p><h2 className="mt-3 font-sora text-3xl font-black text-white">Clear scope before payment.</h2><div className="mt-8 grid gap-4 md:grid-cols-3">{SERVICES.slice(0,3).map((s)=><Link key={s.slug} href={`/services/${s.slug}`} className="rounded-3xl border border-white/10 bg-[var(--qp-surface)] p-5 hover:bg-white/[0.07]"><h3 className="font-sora text-xl font-black text-white">{s.name}</h3><p className="mt-2 text-sm text-muted">{s.description}</p><p className="mt-4 text-2xl font-black text-white">${s.usd}</p><p className="mt-1 text-xs text-muted">Delivery: {s.delivery}</p></Link>)}</div></div></section>; }

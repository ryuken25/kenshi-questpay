import StudioShell from "@/components/StudioShell";
import { requireStudioAdmin } from "@/lib/supabase-auth";
import { ADMIN_EMAIL, hasSMTP, receiveAddressValid } from "@/lib/server-config";

export const dynamic="force-dynamic";
export default async function StudioSettings({searchParams}:{searchParams:{emailTest?:string}}){const user=await requireStudioAdmin();return <StudioShell email={user.email||"owner"}><h1 className="font-sora text-3xl font-black">Studio settings</h1>{searchParams.emailTest&&<p className="mt-4 rounded-2xl border border-green-400/20 bg-green-400/10 p-4 text-sm text-green-200">Email test accepted. Check the email event log for provider result.</p>}<div className="mt-6 grid gap-4 md:grid-cols-3"><Status label="SMTP" ok={hasSMTP}/><Status label="Receiver" ok={receiveAddressValid}/><Status label="Owner allowlist" ok={Boolean(ADMIN_EMAIL)}/></div><section className="mt-6 rounded-[2rem] border border-white/10 bg-[var(--qp-surface)] p-5"><h2 className="font-sora text-xl font-black">Delivery diagnostics</h2><p className="mt-2 text-sm leading-6 text-muted">Sends one controlled test to the allowlisted administrator and logs the attempt. No credentials or recipient address are exposed.</p><form action="/api/studio/email/test" method="post"><button className="mt-4 min-h-12 rounded-2xl bg-verse-purple px-6 font-black">Send admin test email</button></form></section></StudioShell>}
function Status({label,ok}:{label:string;ok:boolean}){return <div className="rounded-2xl border border-white/10 bg-[#111522] p-4"><p className="text-xs uppercase text-muted">{label}</p><p className={`mt-2 font-black ${ok?'text-green-400':'text-red-400'}`}>{ok?'Configured':'Missing'}</p></div>}

import { notFound } from "next/navigation";
import StudioShell from "@/components/StudioShell";
import { requireStudioAdmin } from "@/lib/supabase-auth";
import { getSupabase } from "@/lib/supabase-server";

export const dynamic="force-dynamic";
export default async function StudioOrderDetail({params}:{params:{id:string}}){const user=await requireStudioAdmin();const sb=getSupabase();if(!sb)notFound();const [{data:order},{data:payment},{data:events}]=await Promise.all([sb.from("orders").select("*").eq("id",params.id).single(),sb.from("payments").select("*").eq("order_id",params.id).maybeSingle(),sb.from("questpay_order_events").select("*").eq("order_id",params.id).order("created_at",{ascending:false})]);if(!order)notFound();return <StudioShell email={user.email||"owner"}><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-mono text-sm text-[#8FEAFF]">{order.public_order_id}</p><h1 className="font-sora text-3xl font-black">{order.slug}</h1></div><span className="rounded-full bg-white/10 px-4 py-2 text-sm font-black">{order.status}</span></div><div className="mt-6 grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><section className="space-y-4 rounded-[2rem] border border-white/10 bg-[var(--qp-surface)] p-5"><h2 className="font-sora text-xl font-black">Private brief</h2><Field label="Client" value={order.customer_name}/><Field label="Contact" value={`${order.contact_method||""}: ${order.contact_value||""}`}/><Field label="Project" value={order.project_link}/><Field label="Deadline" value={order.deadline}/><Field label="Main problem" value={order.brief}/><Field label="Expected output" value={order.expected_output}/><Field label="References" value={order.ref_links}/><Field label="Notes" value={order.notes}/></section><section className="space-y-5"><div className="rounded-[2rem] border border-white/10 bg-[var(--qp-surface)] p-5"><h2 className="font-sora text-xl font-black">Lifecycle</h2><form action={`/api/studio/orders/${order.id}/status`} method="post" className="mt-4 grid gap-2"><select name="status" defaultValue={order.status} className="min-h-12 rounded-2xl bg-[rgba(8,11,24,.56)] px-4">{["pending","paid","reviewing","accepted","in_progress","delivered","completed","cancelled"].map(x=><option key={x}>{x}</option>)}</select><button className="min-h-12 rounded-2xl bg-verse-purple font-black">Update status + log event</button></form></div><div className="rounded-[2rem] border border-white/10 bg-[var(--qp-surface)] p-5"><h2 className="font-sora text-xl font-black">Payment snapshot</h2><div className="mt-3 space-y-2 text-sm"><Field label="Expected" value={`${order.amount_human} ${order.token_symbol}`}/><Field label="Receiver" value={order.receive_address}/><Field label="Transaction" value={payment?.tx_hash}/><Field label="Buyer" value={payment?.from_address}/></div></div><div className="rounded-[2rem] border border-white/10 bg-[var(--qp-surface)] p-5"><h2 className="font-sora text-xl font-black">Timeline</h2><div className="mt-3 space-y-2">{(events||[]).map(event=><div key={event.id} className="rounded-xl bg-[rgba(8,11,24,.42)] p-3 text-sm"><b>{event.event_type}</b><p className="text-xs text-muted">{new Date(event.created_at).toLocaleString()}</p></div>)}{!events?.length&&<p className="text-sm text-muted">No events recorded yet.</p>}</div></div></section></div></StudioShell>}
function Field({label,value}:{label:string;value:unknown}){return <div className="rounded-2xl bg-[rgba(8,11,24,.42)] p-4"><p className="text-xs uppercase tracking-wider text-muted">{label}</p><p className="mt-2 whitespace-pre-wrap break-words text-sm text-secondary">{String(value||"—")}</p></div>}
